// Deprecated. Marketing/analytics pixels now load through Google Tag Manager
// (see GoogleTagManager.tsx). Tags are managed in the GTM container, not here.
// This stub is kept only so any stale import does not break the build.
export default function Pixels() {
  return null;
}
